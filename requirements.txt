# Python dependencies for TSP Algorithm Monitoring System

# CSV parsing
# (using built-in csv module, no external dependency needed)

# JSON serialization
# (using built-in json module, no external dependency needed)

# Date and time handling
# (using built-in datetime module, no external dependency needed)

# No external dependencies required - using Python standard library only
